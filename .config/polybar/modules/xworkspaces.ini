[module/ewmh]
type = internal/xworkspaces

label-active = %name%
label-active-underline= ${var.primary}
label-active-padding = 1

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%
label-urgent-background = ${var.primary}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${var.disabled}
label-empty-padding = 1